# Runtime state — the live message bus (logs + per-reader cursors). Never committed.
# Anchored to the repo root: an unanchored `bus/` would also swallow any
# tracked directory named bus (it silently ate .claude/skills/bus/).
/bus/
# Web transport state — local per-reader cursors + cached bus-issue number.
/bus-web/
